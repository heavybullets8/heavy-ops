Vendored from MCXboxBroadcast/Broadcaster @ 202bf81 (build 149, Bedrock 1.26.40),
for image digest sha256:8970858991b51bdbe19d13f6e68da843d99c5ed0e62d520007497feebbc3a428.

One change, marked "heavy-ops". checkConnection() never looked at the signaling
websocket, so when it dropped nothing reconnected it and joins died silently.
Writeup: https://github.com/MCXboxBroadcast/Broadcaster/issues/383

Re-sync when the digest changes:
  git clone --depth 1 https://github.com/MCXboxBroadcast/Broadcaster
  # running build is in the first log line: "Starting MCXboxBroadcast Standalone
  # build N (git-master-SHA)"
  # re-apply the heavy-ops block, bump the digest above

The initContainer is fail-soft: if the patch stops compiling it runs the stock
jar and logs "PATCH FAILED". Nothing crashes, the wedges just come back. Check
for that string after a Renovate bump.
