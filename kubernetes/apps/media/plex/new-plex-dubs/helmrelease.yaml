---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app new-plex-dubs
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn:
    - name: plex
      namespace: media

  values:
    controllers:
      new-plex-dubs:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/heavybullets8/new-plex-dubs
              tag: 1.0.3@sha256:130f772b0d9a4270d6fd3b7db522662165d255069d9abec402d666a02816f7bc
            env:
              TZ: ${TIMEZONE}
              PLEX_URL: http://plex.media.svc.cluster.local:32400
              PLEX_ANIME_SERIES: Anime Series
              PLEX_ANIME_MOVIES: Anime Movies
              PORT: &port 9432
            envFrom:
              - secretRef:
                  name: new-plex-dubs-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
            resources:
              requests:
                cpu: 5m
                memory: 128Mi
              limits:
                memory: 5Gi

    service:
      app:
        controller: *app
        ports:
          tcp:
            port: *port
